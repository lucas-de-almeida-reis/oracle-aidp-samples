"""Optional local configuration for environments without compute variables.

Copy this file to ``deployment_config.py`` and replace the placeholder. The
real ``deployment_config.py`` is ignored by Git because it is environment
specific and must not be contributed.
"""

COMPARTMENT_ID = "<your-compartment-ocid>"
